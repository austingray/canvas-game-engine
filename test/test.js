// tests module; to begin with, I'll import each module directly above its tests. When we need to we can split them 
// const THREE = require('https://ajax.googleapis.com/ajax/libs/threejs/r84/three.min.js');
const assert = require('assert');
describe("Sprites", function() {
    describe("#looping()", function() {
        it('should loop through the frames of this one dimensional sprite', function() {
            assert.equal(1, 1, 'should be equal');
        })
    })
}) 